# ex05 - Agent Repository Exploration

## Goal

Use a coding agent to inspect an unfamiliar Python repository before making
changes.

The agent should determine the program entry point, module responsibilities,
call relationships, data flow and input-validation path.

The source code must not be changed during repository exploration.

After the agent produces its report, its conclusions must be checked manually
using source inspection and executable verification.
